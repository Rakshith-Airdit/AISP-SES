<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <!-- Table for planned items -->
    <Table
        id="productsTablePlanned"
        inset="false"
        items="{selItemModel>/items}"
        class="sapFDynamicPageAlignContent"
        width="100%"
        mode="None"
    >
        <headerToolbar>
            <OverflowToolbar>
                <Text text="Item No/Details: {poDetailModel>/0/Ebelp}" />
                <ToolbarSpacer />
                <Button icon="sap-icon://excel-attachment" press="onExcelExport"/>
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Text text="Sr No" />
            </Column>
            <Column>
                <Text text="Service Number" />
            </Column>
            <Column width="15em">
                <Text text="Service Description" />
            </Column>
            <Column>
                <Text text="Ordered Quantity" />
            </Column>
            <Column>
                <Text text="UOM" />
            </Column>
            <Column>
                <Text text="Unit Price" />
            </Column>
            <Column>
                <Text text="Service Quantity" />
            </Column>
            <Column>
                <Text text="Serviced Quantity" />
            </Column>
            <Column>
                <Text text="Total Price" />
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <Text text="{selItemModel>SR_NO}" />
                    <Text text="{selItemModel>SERVICE_NUMBER}" />
                    <Text text="{selItemModel>SERVICE_DESCRIPTION}" />
                    <Text text="{selItemModel>ORDERED_QUANTITY}" />
                    <Text text="{selItemModel>UNIT_OF_MEASURE}" />
                    <Text text="{selItemModel>UNIT_PRICE}" />
                    <Input
                        id="idPlannedSrvQtyInput"
                        type="Number"
                        value="{selItemModel>newField}"
                        liveChange="onInputChange"
                    />
                    <Text text="{selItemModel>SERVICE_QUANTITY}" />
                    <Text text="{selItemModel>TOTAL_PRICE}" />
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</core:FragmentDefinition>
